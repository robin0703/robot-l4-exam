[{"i":"2048595407495749634","no":1,"t":4,"s":"<p>实操试题(共1题，共100分)</p><p>1. 主题：红外角度指示器</p><p>器件：Atmega328P主控板1块，红外接收套件1套，LED模块1个，电位器模块1个，舵机1个。以上模块也可使用分立器件结合面包板搭建。</p><p>任务要求：红外角度指示器通过旋转电位器控制舵机的转动角度，通过LED灯指示不同的位置。红外角度指示器有两种工作状态：连续状态和刻度状态，两种状态通过红外按键切换。当指示器处于连续状态时，舵机随着电位器的转动而平滑转动；当指示器处于刻度状态时，随着电位器的转动，舵机位置仅位于0度、90度和180度三个位置。具体要求如下：</p><p>(1) 点击红外遥控器的1键，指示器进入连续工作状态。当电位器从一端旋转至另外一端时，舵机的转动角度从0度旋转至180度，LED灯的亮度从熄灭渐变至最亮；</p><p>(2) 点击红外遥控器的2键，指示器进入刻度工作状态。当电位器的返回值小于256时，舵机的转动角度为0度，LED灯熄灭；当电位器的返回值大于等于256且小于768时，舵机的转动角度为90度，LED灯点亮；当电位器的返回值大于等于768时，舵机的转动角度为180度，LED灯闪烁；</p><p>(3) 控制程序采用Arduino C/C++代码编写，不得使用图形化软件编写；</p><p>(4) 未作规定处可自行处理，无明显与事实违背即可。</p><p>&nbsp;</p><p>&nbsp;</p><p>说明：程序编写过程中不得打开其它示例程序，如发现，实操成绩按照0分处理。</p><p>&nbsp;</p><p>请考生在考试结束前，按照如下要求提交相关文件。</p><p>(1) 将程序放在一个文件夹中，压缩为1个“rar或zip”格式文件，并命名为：DJKS4_身份证号，大小5M以下；</p><p>(2) 将程序文件通过“上传附件”按钮进行上传。</p><p>&nbsp;</p><p>&nbsp;</p><p>参考资料：</p><p>&nbsp;</p><p>舵机类库及常用成员函数如下：</p><p>头文件：Servo.h</p><p>类库对象：Servo</p><p>成员函数：attach(Pin)&nbsp;//指定引脚的关联引脚</p><p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;write(angle) //功能是让舵机转动到指定的角度</p><p>红外遥控示例：</p><p>#include &lt;IRremote.h&gt;&nbsp;&nbsp;&nbsp;&nbsp;</p><p>const int RECV_PIN = 12;&nbsp;</p><p>IRrecv irrecv(RECV_PIN);&nbsp;</p><p>decode_results results;&nbsp;&nbsp;</p><p>&nbsp;</p><p>void setup(){</p><p>&nbsp;&nbsp;Serial.begin(9600);&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</p><p>&nbsp;&nbsp;irrecv.enableIRIn();&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</p><p>}</p><p>void loop() {</p><p>&nbsp;&nbsp;if (irrecv.decode(&amp;results)) {&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</p><p>&nbsp;&nbsp;&nbsp;&nbsp;Serial.println(results.value, HEX);&nbsp;</p><p>&nbsp;&nbsp;&nbsp;&nbsp;irrecv.resume();&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</p><p>&nbsp;&nbsp;}</p><p>&nbsp;&nbsp;delay(100);</p><p>}</p><p>&nbsp;</p><p>&nbsp;</p><p>评分项：</p><p>&nbsp;</p><p>1. 器件及器件连接（20分）</p><p>&nbsp;&nbsp;说明：如考生采用的是集成开发板，实操项目所需元器件均预先集成在PCB上，无实际电路搭设操作，电路搭设部分得分为0分。</p><p>2. 功能实现（80分）</p><p>(1) 实现点击红外遥控器的1键，指示器进入连续工作状态后。当电位器从一端旋转至另外一端时，舵机的转动角度从0度旋转至180度；（15分）</p><p>(2) 实现指示器进入连续工作状态后，当电位器从一端旋转至另外一端时，LED灯的亮度从熄灭渐变至最亮；（15分）</p><p>(3) 实现点击红外遥控器的2键，指示器进入刻度工作状态。当电位器的返回值小于256时，舵机的转动角度为0度；（10分）</p><p>(4) 实现指示器进入刻度工作状态，当舵机转动角度为0度时，LED灯熄灭；（5分）</p><p>(5) 实现指示器进入刻度工作状态,当电位器的返回值大于等于256且小于768时，舵机的转动角度为90度；（10分）</p><p>(6) 实现指示器进入刻度工作状态,当舵机转动角度为90度时，LED灯点亮；（5分）</p><p>(7) 指示器进入刻度工作状态,当电位器的返回值大于等于768时，舵机的转动角度为180度；（10分）</p><p>(8) 实现指示器进入刻度工作状态,当舵机转动角度为180度时，LED灯闪烁。（10分）</p>","o":[],"a":"","an":"<p>1. 初始化Atmega328P的I/O引脚，配置红外接收引脚为输入，连接舵机、LED、电位器至指定引脚；2. 包含Servo.h库并创建Servo对象，使用attach()绑定舵机控制引脚；3. 初始化红外接收库，启用红外解码功能；4. 在主循环中检测红外信号，判断是否接收到1键或2键的红外指令，切换工作状态标志变量；5. 读取电位器模拟值，根据当前状态执行对应逻辑：连续状态时，将电位器值线性映射到0-180度角度和0-255亮度，驱动舵机转动并用analogWrite调节LED亮度；6. 刻度状态时，对电位器值进行区间判断：小于256时设置舵机0度、LED灭；256-768间设置舵机90度、LED亮；大于等于768时设置舵机180度、LED闪烁；7. LED闪烁采用周期性翻转电平实现，间隔固定延时。</p>","sc":100.0,"kp":["Arduino UNO/Nano主控板:初始程序,引脚","Arduino UNO/Nano主控板:USB供电,外部电源供电,工作电压,输入电压,输出电压","面包板的构造和使用","Arduino编译环境:Arduino IDE编程","D0引脚RX（接收数据），D1引脚TX（发送数据）,PWM脉冲引脚模拟输出引脚,数字输入引脚,数字输出引脚,模拟输入引脚","delay()函数的基本认识,delay()函数的功能和调用","模拟输出:PWM占空比","电路搭设的基本要求和操作规范","digitalRead()函数的基本认识,digitalRead()函数的功能和调用","选择结构: 如果:if    否则如果 else if, 否则else;    switch语句","数值在二进制、十进制和十六进制之间进行转换","模拟量、数字量、I/O口输入输出等概念","程序设计的三种基本结构、自定义函数","数学、比较及逻辑运算","直流电机运动控制及伺服电机（舵机）控制","已有的一些传感器功能函数的使用","类库概念、安装及使用","通过控制器I/O口实现数字信号、模拟信号的读写","通过编程控制伺服电机（舵机）实现指定转动"]}]